import { Input } from "@/components/ui/input";

const SearchBox = () => {
  return (
    <form>
      <Input placeholder="Search here..." className="h-9 rounded-full bg-gray-50"></Input>
    </form>
  );
};

export default SearchBox;
